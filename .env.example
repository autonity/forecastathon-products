# Requires Python 3.12+
# Setup: python3.12 -m venv .venv && source .venv/bin/activate && pip install -r scripts/requirements.txt

# Filebase IPFS API token
FILEBASE_TOKEN=your_filebase_token_here

# Database configuration for builder registration check
DB_HOST=your_db_host
DB_PORT=your_db_port
DB_NAME=your_db_name
DB_USERNAME=your_db_username
DB_PWD=your_db_password
